$schema: https://azuremlschemas.azureedge.net/latest/managedOnlineEndpoint.schema.json
name: credit-risk-endpoint
description: Endpoint para predição de risco de crédito - Bootcamp Microsoft Azure
tags:
  model_type: "classification"
  use_case: "credit_risk"
  framework: "xgboost" 
  stage: "production"
  version: "v1.0"
  
# Configurações de autenticação
auth_mode: key

# Configurações de tráfego e infraestrutura
traffic:
  blue: 100
  
# Configurações adicionais
properties:
  enforce_access_to_default_secret_stores: false
